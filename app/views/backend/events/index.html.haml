%h4 Events

= link_to "Add event", new_backend_event_path, class: 'btn btn-primary'

- if @events.any?
  %table.table.table-striped
    %thead
      %tr
        %th.column-small Date
        %th Title
        %th.column-small Registrations

    %tbody
      - @events.each do |event|
        %tr
          %td= event.start_time.strftime('%d-%m-%Y')
          %td= link_to event.title, edit_backend_event_path(event)
          %td= link_to "#{event.confirmed_registrations.count} / #{event.number_of_seats}", backend_event_registrations_path(event)
- else
  %p There are no events..
