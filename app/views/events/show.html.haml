%h2= resource.title

= simple_form_for([resource, @new_registration]) do |f|
  = f.input :name
  = f.input :email
  = f.submit 'Register', class: 'btn btn-default'

- @confirmed_registrations.each do |registration|
  %p= registration.name
