.updates
  .container
    .row
      .col-md-12
        %h2 Podcasts

.container
  .row
    .col-md-12

      - @podcasts.each do |podcast|
        %h4= link_to podcast.title, podcast_path(podcast.guid.content)
        .podcast
          %p= raw truncate_html(podcast.description, length: 500)
          .clearfix
        %hr/
